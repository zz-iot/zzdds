.{
    .name = .zzdds_examples_waitset_zig,
    .version = "0.0.0",
    .fingerprint = 0xe65782da75c147c6,
    .minimum_zig_version = "0.16.0",
    .dependencies = .{
        // Same convention as zig/hello_world and zig/shape: point straight at
        // zzdds's own repo root (this example lives at examples/zig/waitset
        // within it) rather than a tagged release URL.
        //
        // No direct dependency on zidl here -- build.zig gets the zidl
        // executable and zidl_rt module *through* zzdds instead (which
        // re-exposes both), so there's only ever one zidl resolution in the
        // whole graph. See build.zig's comment for why a second, independent
        // one breaks the build outright when zidl is a `.path` dependency.
        .zzdds = .{
            .path = "../../..",
        },
    },
    .paths = .{
        "build.zig",
        "build.zig.zon",
        "publisher.zig",
        "subscriber.zig",
        "idl",
    },
}
