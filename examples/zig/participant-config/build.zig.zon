.{
    .name = .zzdds_examples_pconfig_zig,
    .version = "0.0.0",
    .fingerprint = 0x309c8d4d3abc9e31,
    .minimum_zig_version = "0.16.0",
    .dependencies = .{
        // Same convention as zig/hello_world: point straight at zzdds's own
        // repo root (this example lives at examples/zig/participant-config
        // within it) rather than a tagged release URL -- see
        // zig/shape/build.zig.zon's comment.
        //
        // No direct dependency on zidl here -- build.zig gets the zidl
        // executable and zidl_rt module *through* zzdds instead (which
        // re-exposes both), so there's only ever one zidl resolution in the
        // whole graph. See build.zig's comment for why a second, independent
        // one breaks the build outright when zidl is a `.path` dependency.
        .zzdds = .{
            .path = "../../..",
        },
    },
    .paths = .{
        "build.zig",
        "build.zig.zon",
        "publisher.zig",
        "subscriber.zig",
        "idl",
    },
}
